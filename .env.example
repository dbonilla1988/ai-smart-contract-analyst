# Optional environment variables

# NODE_ENV=development

# --- LLM (optional) ---
# Server-side only. Never use NEXT_PUBLIC_ for these.
OPENAI_API_KEY=
# Must be one of: gpt-4o-mini | gpt-4o | gpt-4.1-mini | gpt-4.1
# OPENAI_MODEL=gpt-4o-mini

# Reserved for a future provider adapter
# ANTHROPIC_API_KEY=
